server:
  port: 8080



spring:
#  devtools:
#    restart:
#      enabled: false   #关闭热部署  一面feignclient调用异常
  application:
    name: sdk


#开启mybatis的sql日志
logging:
  level:
    root: info
    com.hualala.sdkdemo: debug
  file: logs/${spring.application.name}.log

